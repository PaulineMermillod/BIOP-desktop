name: "gan4mic"
channels:
  - conda-forge
  - defaults
dependencies:
# for conda install
  - python=3.10
  - seaborn
  - ipykernel
  - pip
  # for pipinstall
  #- pip:
  #    - scikit-image
  #    - dask
prefix: "/opt/conda/envs/gan4mic"